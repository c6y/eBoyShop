$cell: {{ settings.cell }};
$cell-margin: {{ settings.cell-margin }};
$content-min-width: 2 * $cell + 3 * $cell-margin;
$breakpoint: {{ settings.breakpoint }};
$color-header-cart: {{ settings.color-header-cart }};
$color-header-border: darken( $color-header-cart, 3% );

$cell-margin-half: $cell-margin * 0.5;
$cell-margin-2x: $cell-margin * 2;
$cell-margin-3x: $cell-margin * 3;
$cell-margin-4x: $cell-margin * 4;
$cell-margin-5x: $cell-margin * 5;
$cell-margin-8x: $cell-margin * 8;

#appheader {
  width: 0 auto;
  box-sizing: border-box;
  display: flex;
  justify-content: space-between;
  position: relative;
  flex-direction: column;

  #appheadertop {
    position: relative;
    padding: $cell-margin-2x $cell-margin $cell-margin-3x $cell-margin-5x;
    margin: 0;
    box-sizing: border-box;
    text-align: left;

    #logotype {
      padding: 0;
    }
  }

  #carthead {
    margin: 0;
    width: 100%;
    padding: $cell-margin 0;
    text-align: right;
    display: table;

    #breadcrumb {
      float: left;
      margin: 0 0 0 $cell-margin-3x;
      #crumbpath {
        // mobile 2 column width
        @media only screen
          and (max-width: (3 * $cell + 4 * $cell-margin)) {
          display: none;
        }
      }


    }

    #cartcount {
      padding: 0;
      margin: 0 $cell-margin-2x 0 0;
      white-space: nowrap;
    }

    #breadcrumb a,
    #cartcount a {
      padding: 0;
      text-decoration: none;
    }

    #breadcrumb a:hover,
    #cartcount a:hover {
      margin-left: -6px;
      margin-right: -6px;
      padding: 0 6px;
      text-decoration: none;
    }
  }
}
